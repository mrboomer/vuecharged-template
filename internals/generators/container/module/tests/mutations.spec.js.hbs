// import mutations from '../mutations';
// import {  } from '../constants';

describe('{{properCase name}} Mutations', () => {
  it('to have unit tests specified', () => {
    expect(true).toBe(false);
  });
});
